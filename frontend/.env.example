# One URL works as before.
# You can also list multiple candidate backends separated by commas.
# NOVA AI will probe /api/status and prefer the backend that is actually ready.
VITE_API_URL=https://nova-ai-backend-1afi.onrender.com,http://localhost:8000
VITE_APP_NAME=NOVA AI
